import React from 'react';

function Header(){
    // use className instead of class when working with React elements ("html elements")
    return(
        <header className="headerA"> This is the header</header>
    )
}
export default Header;